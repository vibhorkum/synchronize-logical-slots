# synchronize_logical_slots extension
comment = 'utility for logical slot synchronization on standby'
default_version = '1.0'
module_pathname = '$libdir/synchronize_logical_slots'
relocatable = true
requires = 'dblink'

